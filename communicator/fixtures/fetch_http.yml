clientId: "clientId"
version: "1.0"
get:
    communicator:
        type: "http"
        method: "GET"
http_capital:
    communicator:
        type: "HTTP"

broken:
    communicator:
        type: "broken"
